## Natural Language

This chapter groups notes on PostgreSQL features that work with human language rather than only exact raw text.

### Topics
1. [[Full-Text Search]]

### Why this chapter exists

The note `Full-Text Search.md` explains how `lec05.sql` moves from simple string splitting to natural-language-aware indexing with stop words, stemming, `tsvector`, `tsquery`, and `GIN` indexes.

